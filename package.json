{
  "name": "collective-bucket-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Collective Bucket merkezi kimlik doğrulama ve SSO uygulaması.",
  "scripts": {
    "serve": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting:auth",
    "stamp": "node scripts/stamp-assets.js",
    "check": "node --check public/client.js && node --check public/assets/auth.js && node --check public/assets/session-bridge.js"
  },
  "devDependencies": {
    "firebase-tools": "^15.27.0"
  }
}
